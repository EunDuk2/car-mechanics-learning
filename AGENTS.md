# 프로젝트 작업 지침

## 모든 Git 작업 전 필수

1. 저장소 루트의 `GIT_WORKFLOW.md`를 읽고 현재 명령에 해당하는 규칙을 확인한다. 조회 명령(status, diff, log, branch 등)도 예외가 아니다.
2. Git 명령은 루트의 `./scripts/git`으로 실행한다. 하위 폴더에서는 해당 스크립트의 상대·절대 경로를 쓴다. `npm run git -- ...`도 허용한다. 래퍼는 실행 전 문서 전문을 표시한다.
3. 직접 Git 바이너리, 별칭, 셸 중첩, 훅 비활성화로 규칙을 우회하지 않는다. GitHub 도구 호출에도 먼저 문서를 확인한다.
4. Git 관련 설치 스크립트·훅·검증 코드는 재귀 방지를 위해 내부적으로 Git 바이너리를 호출할 수 있다. 이를 일반 Git 작업의 우회 수단으로 사용하지 않는다.
5. CI는 checkout 이후 별도 단계에서 `GIT_WORKFLOW.md`를 표시하고 버전 관리된 검증 스크립트를 실행한다.

## 프로젝트 확인

- Vite + Three.js 기반 자동차 구조·작동 학습 및 엔진 조립 실습이다.
- 변경에 맞는 검증을 실행한다. 명령과 범위는 `GIT_WORKFLOW.md` 및 `README.md`를 따른다.
- 기존 3D 자산의 출처와 사용자 학습 기록의 저장 호환성을 보존한다.

## 커밋 작성자

- 모든 커밋의 Author와 Committer는 `.git-author.json`의 사용자 계정만 사용한다.
- 에이전트 이름·봇 이메일·Co-authored-by·자동 생성 서명을 추가하지 않는다.
- 커밋 후 메타데이터와 메시지를 확인한 다음 푸시한다.
- 사용자가 요청한 커밋 개수만 처리한다. 나머지 변경은 작업 트리에 유지한다.
